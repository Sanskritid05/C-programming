#include <stdio.h>

int main() {
    int num;
    scanf("%d", &num);

    int fact = 1;  
    int i = 1;
    
    while(fact < num){
      i++;
      fact *= i;
    }
    if (fact == num) {
        printf("%d", i);  
    } 
    else {
        printf("NO"); 
    }

    return 0;
}
